# Deprecated path alias

Auxiliary-agent shared rules now live under:

`Shared documents/auxiliary-agents/`

(load `system_rules.md`, `proposal_schema.md`, `initialization_protocol.md`, `enforcement_rules.md` from that folder).
