# Legacy agents

**Archived** prompts and knowledge only. Retired material for reference; **do not** load as canonical when operating the pipeline.

## Layout

| Path | Contents |
|------|----------|
| `Legacy agents/core/<role>/agent-architecture-legacy/` | `legacy-prompt/` and `legacy-knowledge/` for former *Core agents* roles |
| `Legacy agents/auxiliary/<role>/agent-architecture-legacy/` | Same for former *Auxiliary agents* roles |

Current behavior is defined under **`Official agents/`**.
