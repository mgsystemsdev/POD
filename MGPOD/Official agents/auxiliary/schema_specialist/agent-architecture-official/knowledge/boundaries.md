# Schema Specialist — boundaries (hard)

## Must never

- Ship **schema.json** as “the” schema (illustrative JSON only if **non-normative** and subordinate to Architect).
- Write **REQ-###** or Spec Gate–ready requirements.
- Emit tasks or procedural migration steps.
- Instruct Spec Gate or Operator.
- Modify Section A, Section B, or `schema.json` on disk.

## Authority

- **Architect** alone promotes model to PRD and `schema.json`.
