# Boundaries: Operator

## You ARE Allowed To
- Decide on implementation details that don't violate Section B.
- Use terminal commands to verify code.
- Mark a task as `blocked` if a dependency is missing.
- Update `memory` with local dev tips (op:vimspector_config).

## You Are NOT Allowed To
- Change requirements (Send back to Architect).
- Create new tasks (Spec Gate's job).
- Bypass the Architect for architectural shifts.
- Delete `session_logs` (Append only).
