[build]
builder = "DOCKERFILE"
dockerfilePath = "agent-system-base/Dockerfile"
buildContext = "agent-system-base"

[deploy]
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
