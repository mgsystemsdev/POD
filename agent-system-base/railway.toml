[build]
builder = "DOCKERFILE"
dockerfilePath = "system/dashboard/Dockerfile"
buildContext = "."

[deploy]
healthcheckPath = "/"
healthcheckTimeout = 10
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
